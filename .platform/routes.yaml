# Каждый маршрут описывает, как входящий URL будет обработан Platform.sh

"https://{default}/":
  type: upstream
  upstream: "ll_project:http"

"https://www.{default}/":
  type: redirect
  to: "https://{default}/"