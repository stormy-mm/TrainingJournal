name: "ll_project"
type: "python:3.14"

relationships:
  database: "db:postgresql"

# Конфигурация приложения при его публикации

web:
  upstream:
    socket_family: unix
  commands:
    start: "gunicorn -w 4 -b unix:$SOCKET ll_project.wsgi:application"
  locations:
    "/":
      passthru: true
    "/static":
      root: "static"
      expires: 1h
      allow: true

# Размер постоянного диска приложения
disk: 512

# Установка локального монтирования для чтения/записи журналов
mounts:
  "logs":
    source: local
    source_path: logs

# Хуки, выполняемые в различные моменты жизненного цикла приложения
hooks:
  build: |
    pip install --upgrade pip
    pip install -r requirements.txt
    pip install -r requirements_remote.txt
    
    mkdir logs
    python manage.py collectstatic
    rm -rf logs
  deploy: |
    python manage.py migrate